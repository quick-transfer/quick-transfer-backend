# =========================
# APPLICATION NAME
# =========================
spring.application.name=quick-transfer
server.port=8080
server.servlet.context-path=/api

# =========================
# DATABASE - POSTGRESQL
# =========================
spring.datasource.url=jdbc:postgresql://postgres:5432/quick_transfer
spring.datasource.username=backend
spring.datasource.password=postgresqlPW
spring.datasource.driver-class-name=org.postgresql.Driver

# =========================
# JPA / HIBERNATE
# =========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# =========================
# SQL INIT
# =========================
spring.sql.init.mode=never

# =========================
# JSON
# =========================
spring.jackson.time-zone=America/Sao_Paulo
spring.jackson.serialization.indent-output=true

# =========================
# JWT
# =========================
app.jwt.secret=feww4g7h3q79gn0n9qun3m8f43ngviv0v43gj5hn
app.jwt.expiration=86400000

# =========================
# JWT (Asymmetric RSA Keys)
# =========================
app.security.jwt.public-key=classpath:certs/public.pem
app.security.jwt.private-key=classpath:certs/private.pem

# =========================
# SECURITY
# =========================
spring.security.filter.dispatcher-types=async,request,error
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=never

# =========================
# CORS
# =========================
app.cors.allowed-origins=*
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.cors.allowed-headers=Authorization,Content-Type,Accept
app.cors.allow-credentials=true

# =========================
# LOGS
# =========================
logging.level.root=INFO
logging.level.org.springframework=INFO
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

# =========================
# ACTUATOR
# =========================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# =========================
# MAIL SENDER
# =========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=quick.transfer.gmail@gmail.com
spring.mail.password=ozyhmjniygwyjtix
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
